# UnitfulVolume { #flood_adapt.unit_system.UnitfulVolume }

```python
unit_system.UnitfulVolume()
```

Combination of volume and unit.

## Attributes {.doc-section .doc-section-attributes}

<code>[**value**]{.parameter-name} [:]{.parameter-annotation-sep} [[float](`float`)]{.parameter-annotation}</code>

:   The volume value, must be greater than or equal to 0.

<code>[**units**]{.parameter-name} [:]{.parameter-annotation-sep} [[UnitTypesVolume](`flood_adapt.objects.forcing.unit_system.UnitTypesVolume`)]{.parameter-annotation}</code>

:   The unit of volume.
