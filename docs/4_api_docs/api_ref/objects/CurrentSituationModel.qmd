# CurrentSituationModel { #flood_adapt.objects.CurrentSituationModel }

```python
objects.CurrentSituationModel()
```

The accepted input for a current situation in FloodAdapt.

## Attributes {.doc-section .doc-section-attributes}

<code>[**projection**]{.parameter-name} [:]{.parameter-annotation-sep} [[str](`str`)]{.parameter-annotation}</code>

:   The name of the projection. Should be a projection saved in the database.

<code>[**year**]{.parameter-name} [:]{.parameter-annotation-sep} [[int](`int`)]{.parameter-annotation}</code>

:   The year of the current situation.
