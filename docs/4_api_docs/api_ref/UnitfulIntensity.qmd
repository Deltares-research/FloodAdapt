# UnitfulIntensity { #flood_adapt.unit_system.UnitfulIntensity }

```python
unit_system.UnitfulIntensity()
```

Combination of intensity and unit.

## Attributes {.doc-section .doc-section-attributes}

<code>[**value**]{.parameter-name} [:]{.parameter-annotation-sep} [[float](`float`)]{.parameter-annotation}</code>

:   The intensity value, must be greater than or equal to 0.

<code>[**units**]{.parameter-name} [:]{.parameter-annotation-sep} [[UnitTypesIntensity](`flood_adapt.objects.forcing.unit_system.UnitTypesIntensity`)]{.parameter-annotation}</code>

:   The unit of intensity.
